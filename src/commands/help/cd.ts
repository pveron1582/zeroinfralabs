export const help_cd = `cd - Change directory

Usage:
  cd [directory]

Examples:
  cd /etc                            # Go to /etc
  cd ..                              # Parent directory
  cd ~                               # User's home
  cd                                 # User's home

Description:
  Changes the current working directory.`;

export const help_su = `su - Switch user

Usage:
  su [username]

Examples:
  su kali       # Switch to kali user
  su            # Switch to root
  exit          # Return to previous user

Description:
  Switches the current user. Requires the user to exist in /etc/passwd.
  Use 'exit' to return to the previous user.`;

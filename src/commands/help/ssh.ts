export const help_ssh = `ssh - SSH client

Usage:
  ssh user@host [-p port]

Examples:
  ssh user@192.168.1.10              # Connect via SSH

Description:
  Connects to a remote host via SSH. Uses discovered credentials
  from hydra or other enumeration.`;

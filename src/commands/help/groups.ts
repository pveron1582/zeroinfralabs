export const help_groups = `groups - Show group membership

Usage:
  groups [username]

Examples:
  groups                            # Show current user's groups
  groups root                       # Show root's groups

Description:
  Displays the groups the current user (or specified user) belongs to.`;

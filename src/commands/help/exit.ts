export const help_exit = `exit - Close session

Usage:
  exit

Description:
  If in a su session, returns to the previous user.
  If in an SSH session, closes it and returns to the attacker machine.
  If on the attacker machine, closes the terminal.`;

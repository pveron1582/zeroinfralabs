export const help_touch = `touch - Create empty files

Usage:
  touch file...

Examples:
  touch newfile.txt                  # Create empty file

Description:
  Creates one or more empty files. If the file exists, updates its timestamp.
  Verifies write permission on the parent directory.`;

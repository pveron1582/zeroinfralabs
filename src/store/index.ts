// ── store/index.ts ─────────────────────────────────────────────────
// Re-exporta el store para facilitar imports

export { useScenarioStore, selectScenario, selectMachines, selectMissions, selectActiveMachine, selectIsWebScenario } from './scenarioStore';
